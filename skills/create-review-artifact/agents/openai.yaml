interface:
  display_name: "Create Review Artifact"
  short_description: "Create and reconnect review artifacts"
  default_prompt: "Use $create-review-artifact to coordinate the explicitly requested review artifact and its review lifecycle."
