# Personal Access Token for publishing to the VS Code Marketplace
# Used by: npm run publish:vscode (vsce publish)
VSCE_PAT=

# Personal Access Token for publishing to the Open VSX Registry
# Used by: npm run publish:ovsx (npx ovsx publish)
OVSX_PAT=

# GitHub Personal Access Token (or Actions secrets.GITHUB_TOKEN)
# Used by: GitHub Actions release workflow and local gh CLI
GITHUB_TOKEN=
